class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        n = len(nums)
        result = [1] * n
        product = 1
        for i in range(1,n):
            product*=nums[i-1]
            result[i] = product
        product = 1
        for j in range(n-2,-1,-1):
            product*=nums[j+1]
            result[j] *= product
        return result
        
