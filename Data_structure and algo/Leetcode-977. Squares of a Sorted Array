class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        output = []
        op = 1
        for v in nums:
            op = v*v
            output.append(op)
        output.sort()
        return output
