class Solution:
    def maximumPopulation(self, logs: List[List[int]]) -> int:
        years = [0]*101
        for birth,death in logs:
            years[birth-1950] +=1
            years[death-1950] -=1
        for i in range(1,101):
            years[i]+=years[i-1]
        max_value = max(years)
        max_index = years.index(max_value) 
        return 1950 + max_index
