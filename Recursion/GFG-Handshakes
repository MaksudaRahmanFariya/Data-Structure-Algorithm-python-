#User function Template for python3

class Solution:
    def count(self, N):
        # code here
        if N == 0:
            return 1
        if N==2:
            return 1
        total = 0
        for i in range(0,N-1,2):
            total += self.count(i) * self.count(N-2-i)
        return total
